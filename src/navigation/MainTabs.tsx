import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import BottomTabNavigator from '../components/Navigation-Component/BottomTabNavigator'; // Import the BottomTabNavigator

const AppNav = () => {
  return (
    
      <BottomTabNavigator /> 

  );
};

export default AppNav;
