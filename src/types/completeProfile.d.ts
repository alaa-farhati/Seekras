 interface Hobby {
  id: number;
  name: string;
}

 interface Interest {
  id: number;
  name: string;
} interface ProgressBarProps {
  totalSteps: number;
  currentStep: number;
  theme: {
    accent: string;
    inputBackground: string;
  };
}