// index.ts

import { fonts } from "./fonts"; // Import fonts
import { sizes } from "./sizes"; // Import sizes
import { colors } from "./colors"; // Import colors



// Export everything for use across the app
export { fonts, sizes, colors };